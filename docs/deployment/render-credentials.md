# Render Deployment — Credentials Reference

**No secrets stored here.** This file documents where credentials live.

## Render

| Credential | Where |
|-----------|-------|
| API Key | `~/.render/cli.yaml` → `api.key` |
| Refresh Token | `~/.render/cli.yaml` → `api.refreshtoken` |
| Workspace ID | `tea-d9freo6rnols73cj90u0` |

**Service IDs:**
| Resource | ID | Dashboard URL |
|----------|-----|---------------|
| Backend | `srv-d9frkhgk1i2s73be0j50` | https://dashboard.render.com/web/srv-d9frkhgk1i2s73be0j50 |
| Admin | `srv-d9ft1t8okrbs738q9f60` | https://dashboard.render.com/web/srv-d9ft1t8okrbs738q9f60 |
| PostgreSQL | `dpg-d9frkbjbc2fs73bq2ncg-a` | https://dashboard.render.com/d/dpg-d9frkbjbc2fs73bq2ncg-a |
| Redis | `red-d9frkeernols73cji320` | https://dashboard.render.com/keyvalue/red-d9frkeernols73cji320 |
| Deploy | `dep-d9frs30k1i2s73begrm0` | — |

## Cloudflare R2

| Credential | Where |
|-----------|-------|
| Access Key ID | Set in Render env var `R2_ACCESS_KEY_ID` |
| Secret Access Key | Set in Render env var `R2_SECRET_ACCESS_KEY` |
| Endpoint | `9e4b33ed2e3cfb1fbbb837ada2399a6d.r2.cloudflarestorage.com` |
| Bucket | `nomnom-images` |
| Prefix | `production` |

⚠️ **Rotate after sharing in chat!** The R2 credentials were shared in the conversation and should be recreated in Cloudflare Dashboard → R2 → API Tokens.

## Firebase

| Credential | Where |
|-----------|-------|
| Service Account | Render secret file `firebase-credentials.json` |
| Project ID | `nomnom-cfe32` |
| Client Email | `firebase-adminsdk-fbsvc@nomnom-cfe32.iam.gserviceaccount.com` |
| Local copy | `backend/config/firebase-credentials.json` |

## Database

| Credential | Where |
|-----------|-------|
| Connection string | Set in Render env var `DATABASE_URL` |
| User | `nomnom` |
| Password | Auto-generated by Render (see `~/.render/cli.yaml` or Render dashboard) |
| DB Name | `nomnom_bd9o` (auto-suffixed by Render) |
| External Host | `dpg-d9frkbjbc2fs73bq2ncg-a.singapore-postgres.render.com:5432` |

## Redis

| Credential | Where |
|-----------|-------|
| Connection string | Set in Render env var `REDIS_URL` |
| User | `red-d9frkeernols73cji320` |
| Password | Auto-generated by Render (see connection-info endpoint) |
| External Host | `singapore-keyvalue.render.com:6379` |

## Admin User

| Field | Value |
|-------|-------|
| Email | `admin@nomnom.lk` |
| Password | `Admin@123` (change for production) |
| Role | `admin` |
| Email Verified | Yes (fixed via DB after initial bootstrap) |

## Docker Hub

| Credential | Where |
|-----------|-------|
| Image | `docker.io/namal97/nomnom-backend:latest` |
| Admin image | `docker.io/namal97/nomnom-admin:latest` |
| GitHub Actions pushes to | Both images on every master merge after tests pass |

## How to Update Credentials

### R2 Keys
1. Create new token at Cloudflare → R2 → API Tokens
2. Update on Render:
   ```bash
   curl -X PUT "https://api.render.com/v1/services/srv-d9frkhgk1i2s73be0j50/env-vars/R2_ACCESS_KEY_ID" \
     -H "Authorization: Bearer $(grep 'key:' ~/.render/cli.yaml | awk '{print $2}')" \
     -H "Content-Type: application/json" \
     -d '{"value": "NEW_KEY"}'
   ```
3. Trigger redeploy:
   ```bash
   curl -X POST "https://api.render.com/v1/services/srv-d9frkhgk1i2s73be0j50/deploys" \
     -H "Authorization: Bearer $(grep 'key:' ~/.render/cli.yaml | awk '{print $2}')" \
     -H "Content-Type: application/json" \
     -d '{}'
   ```

### Admin Password
1. Update on Render (env var `ADMIN_PASSWORD`)
2. Trigger redeploy
3. The admin bootstrap only creates if no admin exists — existing admin password is set at creation time, not from env var at runtime. To change, update directly in DB or recreate the user.
